- name: web server
  hosts: web
  become: yes
  tasks:
    - name: install nginx
      ansible.builtin.apt:
        name: "nginx"
        state: "present"
    - name: start nginx
      ansible.builtin.service:
        name: "nginx"
        state: "started"